# GATES — psychic-repurpose

| Gate | Scope | Evidence | Status |
|---|---|---|---|
| RPG-0 | Birth: ten blueprints spanning the program's proven patterns, validator binding sections + provenance + counts with existence-first controls | Born under the parent psychic-crew HELIX gate SIDE-4; closure rides the same operator token, stamped in both ledgers | **APPROVED** `APPROVE SIDE-4` @ 2026-08-27T01:45:18Z |
| VIS-RECONCILE | 2026-08-31 | RETROACTIVE RECORD, append-only: this repo was flipped PUBLIC out-of-band on GitHub after birth — no token existed, no commit marks it, the flip date is unrecorded. Ratified public at S0-RECONCILE: the estate hazard audit (paths, identities, employer refs) returned CLEAN, enforced by this repo's own validator hygiene arms | recorded without a token; grandfathered from the explainer law by enumeration in docs/explainers/INDEX.md | recorded |
| S0-RECONCILE |  | MIT LICENSE + canonical identity (Nathan Lim) + explainer-epoch port + visibility ratified (VIS-RECONCILE) | multi-repo gate (one token, per-repo stamps after per-repo push verification); append-only history law held | **APPROVED** `APPROVE S0-RECONCILE` @ 2026-08-31T23:39:46Z |
| RPG-1 |  | the graph: 11th blueprint born (explainer-epoch); frontmatter onto all 11 (id=stem, one defect-class slug each — vocabulary RATIFIED by this token, triggers >=3, requires DAG, proven_in tied to the prose); Kills table literalized to slug-dash-prose so agreement is extraction not interpretation; docs/PULL-INDEX.md generated (closure precomputed) + docs/PULL-PROTOCOL.md with trigger-matching and path-not-body DECLARED UNASSERTED (the one real bound lands at RPG-2) and the RSCH-1 graph-pilot NOT discharged | suite 30 to 75/0: C2 integrity over the SOURCE (bijection, Kahn acyclicity, Kills literal both ways), C3 faithful projection (independent re-derivation, closure fixed point WITH POSITIVE WITNESS — its first form died on an awk reserved word and passed vacuously, caught in-build — drift arm, emptied-block + dropped-edge probes), every-occurrence README bindings fire-probed, third fixture broken three ways with isolation | revert unit: frontmatter+index+README together (count bindings forbid partial revert); pin: RPG-2 | **APPROVED** `APPROVE RPG-1` 2026-09-01T03:20Z (operator, exact token; 11-slug vocabulary RATIFIED by this token) |
| S5-README-UX |  | repurpose leg: Kills table LINKED (C2 extractor updated same commit), the requires graph RENDERED in README and BOUND — C4 re-derives edges from PULL-EDGES and set-compares, dropped-edge control fires; bound blueprints badge (§C extractor extended); honesty section | 78/0; closes third | **APPROVED** `APPROVE S5-README-UX` 2026-09-01T06:05Z (operator, exact token; leg 3 of 6) |
